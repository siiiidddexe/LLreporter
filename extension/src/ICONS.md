# Icons

Drop 16/48/128 px PNGs here named `icon16.png`, `icon48.png`, `icon128.png`.

A quick placeholder: any solid blue square with a white "LL" glyph works.
Until you add them Chrome will use the default puzzle icon — the extension still works.
