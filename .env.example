# ---- Web app ----
DATABASE_URL=postgresql://llreporter:llreporter@db:5432/llreporter
NEXTAUTH_SECRET=replace-me-with-openssl-rand-base64-32
PUBLIC_URL=https://webaudit.logiclaunch.in
NODE_ENV=production

# ---- Seed super-admin (first boot only) ----
SEED_ADMIN_EMAIL=admin@logiclaunch.in
SEED_ADMIN_PASSWORD=changeme
SEED_ADMIN_NAME=Super Admin

# ---- Postgres (docker-compose) ----
POSTGRES_USER=llreporter
POSTGRES_PASSWORD=llreporter
POSTGRES_DB=llreporter
